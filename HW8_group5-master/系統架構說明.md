# 台北咖啡廳搜尋引擎 - 系統架構說明

## 📐 完整系統架構

```
┌─────────────────────────────────────────────────────────────────┐
│                        使用者介面 (Frontend)                      │
│                         http://localhost:8080                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  index.html                        favorites.html              │
│  ├── 搜尋框                         ├── 收藏清單                │
│  ├── 地區篩選                       └── 移除收藏                │
│  ├── 功能篩選                                                   │
│  ├── 搜尋結果展示                                               │
│  └── 熱門推薦                                                   │
│                                                                 │
│  JavaScript Modules:                                            │
│  ├── app.js          (主應用程式)                               │
│  ├── search.js       (搜尋管理)                                 │
│  ├── filter.js       (篩選管理)                                 │
│  ├── favorite.js     (收藏管理 - LocalStorage)                  │
│  ├── history.js      (歷史紀錄 - LocalStorage)                  │
│  └── utils.js        (工具函數)                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                ↕ HTTP REST API
┌─────────────────────────────────────────────────────────────────┐
│                      控制層 (Controller)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  SearchController.java                                          │
│  ├── GET  /api/search?q=關鍵字                                  │
│  └── GET  /api/search/advanced?keyword=...&districts=...       │
│                                                                 │
│  RecommendationController.java                                  │
│  └── GET  /api/recommendations?districts=...&features=...      │
│                                                                 │
│  FilterController.java                                          │
│  ├── GET  /api/filters/districts                               │
│  └── GET  /api/filters/features                                │
│                                                                 │
│  ErrorReportController.java                                     │
│  └── POST /api/error-reports                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────────┐
│                      服務層 (Service)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  SearchService.java (主要搜尋邏輯)                               │
│  ├── search(keyword) → 整合所有搜尋流程                          │
│  ├── advancedSearch(keyword, filters) → 進階篩選                │
│  └── 呼叫 KeywordService、RankingService、HashtagService        │
│                                                                 │
│  KeywordService.java (關鍵字管理)                                │
│  ├── 載入 keywords.json                                         │
│  ├── getTier1Keywords() → 高權重關鍵字 (2.0-3.0)                │
│  ├── getTier2Keywords() → 中權重關鍵字 (1.0-1.9)                │
│  ├── getTier3Keywords() → 低權重關鍵字 (0.5-1.0)                │
│  └── getKeywordWeight(keyword) → 取得關鍵字權重                 │
│                                                                 │
│  RankingService.java (排名計算)                                  │
│  ├── calculateScore(cafe, keywords) → 計算咖啡廳分數            │
│  ├── rankResults(cafes, keywords) → 排序結果                    │
│  └── 公式: score = Σ(關鍵字出現次數 × 權重)                     │
│                                                                 │
│  HashtagService.java (Hashtag 生成)                             │
│  ├── generateHashtags(cafe, userInput) → 生成 hashtags         │
│  ├── 從使用者輸入抽取 2 個關鍵詞                                 │
│  ├── 從咖啡廳內容抽取 3 個高頻 domain keywords                   │
│  └── 合併並過濾成 3-5 個 hashtags                                │
│                                                                 │
│  PreviewService.java (預覽生成)                                  │
│  └── generatePreview(cafe) → 生成咖啡廳預覽文字                 │
│                                                                 │
│  FilterService.java (篩選服務)                                   │
│  ├── filterByDistricts(cafes, districts)                       │
│  ├── filterByFeatures(cafes, features)                         │
│  └── getAvailableFilters()                                     │
│                                                                 │
│  RecommendationService.java (推薦服務)                           │
│  ├── getTopRecommendations() → 基於 baseline score             │
│  └── getRecommendationsByFilter(filters)                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────────┐
│                    核心引擎層 (Core)                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  SearchEngine.java (搜尋引擎核心)                                │
│  ├── 整合所有核心組件                                            │
│  ├── 協調搜尋流程                                                │
│  └── 管理搜尋狀態                                                │
│                                                                 │
│  Ranker.java (排名器)                                            │
│  ├── rank(pages, query) → 計算並排序網頁                        │
│  ├── quickSort() → 快速排序演算法                                │
│  └── 資料結構: ArrayList<WebPage>                               │
│                                                                 │
│  KeywordParser.java (關鍵字解析器)                               │
│  ├── parse(userInput) → 解析使用者輸入                          │
│  ├── tokenize() → 中文分詞                                      │
│  ├── filterStopWords() → 過濾停用詞                             │
│  └── matchDomainKeywords() → 比對 domain keywords              │
│                                                                 │
│  HashtagGenerator.java (Hashtag 生成器)                         │
│  ├── generate(content, userKeywords) → 生成 hashtags           │
│  ├── extractTopKeywords() → 抽取高頻關鍵字                      │
│  └── combineAndFilter() → 合併並過濾                            │
│                                                                 │
│  BaselineScoreCalculator.java (基準分數計算器)                   │
│  ├── calculateBaseline(cafe) → 計算咖啡廳基準分數               │
│  └── 考慮所有關鍵字的綜合評分                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────────┐
│                    工具層 (Util)                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ChineseTokenizer.java (中文分詞器)                              │
│  ├── tokenize(text) → 將中文文本分詞                            │
│  └── 支援中文、英文、數字的混合分詞                              │
│                                                                 │
│  StopWordsFilter.java (停用詞過濾器)                             │
│  ├── filter(tokens) → 過濾停用詞                                │
│  ├── 載入 stopwords.txt                                         │
│  └── 停用詞: 的、是、在、等、有、和、與...                       │
│                                                                 │
│  ValidationUtil.java (驗證工具)                                  │
│  ├── validateSearchInput(input) → 驗證搜尋輸入                  │
│  └── checkRelevancy(keywords) → 檢查相關性閾值                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────────┐
│                    資料存取層 (Repository)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  CafeRepository.java (咖啡廳資料存取)                            │
│  ├── findAll() → 取得所有咖啡廳                                  │
│  ├── findById(id) → 根據 ID 查詢                                │
│  ├── findByDistrict(district) → 根據地區查詢                    │
│  └── 載入 cafes.json (30 家咖啡廳)                               │
│                                                                 │
│  KeywordRepository.java (關鍵字資料存取)                         │
│  ├── findAll() → 取得所有關鍵字                                  │
│  ├── findByTier(tier) → 根據 tier 查詢                          │
│  └── 載入 keywords.json (30 個關鍵字)                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────────┐
│                    資料模型層 (Model)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Cafe.java (咖啡廳模型)                                          │
│  ├── id: String                                                 │
│  ├── name: String                                               │
│  ├── url: String                                                │
│  ├── district: String                                           │
│  ├── address: String                                            │
│  ├── keywords: List<String>                                     │
│  ├── features: List<String>                                     │
│  ├── hashtags: List<String>                                     │
│  ├── preview: String                                            │
│  └── score: double                                              │
│                                                                 │
│  Keyword.java (關鍵字模型)                                       │
│  ├── keyword: String                                            │
│  ├── weight: double                                             │
│  ├── tier: KeywordTier (TIER1/TIER2/TIER3)                     │
│  └── translations: Map<String, String>                         │
│                                                                 │
│  SearchResult.java (搜尋結果模型)                                │
│  ├── cafe: Cafe                                                 │
│  ├── score: double                                              │
│  ├── matchedKeywords: List<String>                             │
│  └── hashtags: List<String>                                     │
│                                                                 │
│  WebPage.java (網頁模型)                                         │
│  ├── url: String                                                │
│  ├── name: String                                               │
│  ├── score: double                                              │
│  └── children: List<WebPage> (用於多層爬取)                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                ↕
┌─────────────────────────────────────────────────────────────────┐
│                    資料檔案 (Data Files)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  src/main/resources/data/                                       │
│  ├── cafes.json               (30 家咖啡廳完整資料)              │
│  ├── keywords.json            (30 個關鍵字與權重)                │
│  ├── baseline-scores.json     (預計算的基準分數)                 │
│  ├── features.json            (12 種功能列表)                    │
│  ├── districts.json           (12 個台北市行政區)                │
│  ├── keyword-translations.json (多語言對照表)                    │
│  └── stopwords.txt            (停用詞列表)                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 搜尋流程圖

```
使用者輸入關鍵字: "不限時 安靜"
        ↓
┌──────────────────────────────────────────┐
│ 1. 前端 (search.js)                       │
│    - 捕獲使用者輸入                        │
│    - 呼叫 API: /api/search?q=不限時 安靜   │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 2. 控制層 (SearchController.java)        │
│    - 接收 HTTP GET 請求                   │
│    - 參數驗證                             │
│    - 呼叫 SearchService.search()          │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 3. 服務層 (SearchService.java)           │
│    - 呼叫 KeywordParser 解析關鍵字         │
│    - 取得所有咖啡廳資料                    │
│    - 計算每家咖啡廳的分數                  │
│    - 排序結果                             │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 4. 關鍵字解析 (KeywordParser.java)        │
│    輸入: "不限時 安靜"                     │
│    ├── ChineseTokenizer.tokenize()       │
│    │   結果: ["不限時", "安靜"]            │
│    ├── StopWordsFilter.filter()          │
│    │   結果: ["不限時", "安靜"] (無停用詞)  │
│    └── KeywordService.matchKeywords()    │
│        結果: [                            │
│          {keyword:"不限時", weight:3.0},  │
│          {keyword:"安靜", weight:2.8}     │
│        ]                                 │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 5. 分數計算 (RankingService.java)        │
│    對每家咖啡廳:                           │
│    ├── 檢查是否包含 "不限時"               │
│    │   出現 2 次 × 3.0 = 6.0 分           │
│    ├── 檢查是否包含 "安靜"                 │
│    │   出現 1 次 × 2.8 = 2.8 分           │
│    └── 總分 = 6.0 + 2.8 = 8.8 分          │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 6. Hashtag 生成 (HashtagService.java)    │
│    ├── 從使用者輸入抽取: ["不限時","安靜"]  │
│    ├── 從咖啡廳抽取高頻詞: ["有插座","wifi"]│
│    └── 合併: ["不限時","安靜","有插座"]     │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 7. 結果排序 (Ranker.java)                 │
│    使用快速排序 (QuickSort):               │
│    ├── 咖啡廳A (分數: 8.8)                 │
│    ├── 咖啡廳B (分數: 6.5)                 │
│    └── 咖啡廳C (分數: 4.2)                 │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 8. 回傳結果 (SearchResult.java)           │
│    {                                     │
│      "success": true,                    │
│      "total": 15,                        │
│      "results": [                        │
│        {                                 │
│          "name": "羊毛與花",              │
│          "score": 8.8,                   │
│          "address": "台北市大安區...",    │
│          "preview": "永康街巷弄內...",    │
│          "hashtags": ["不限時","安靜"]    │
│        },                                │
│        ...                               │
│      ]                                   │
│    }                                     │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────┐
│ 9. 前端渲染 (app.js)                      │
│    - 顯示搜尋結果卡片                      │
│    - 顯示分數、地址、hashtags              │
│    - 綁定收藏按鈕事件                      │
└──────────────────────────────────────────┘
```

---

## 🎯 關鍵字權重系統

```
┌─────────────────────────────────────────┐
│          Domain Keywords (30 個)         │
├─────────────────────────────────────────┤
│                                         │
│  Tier 1 (15 個) - 權重: 2.0 ~ 3.0        │
│  ┌───────────────────────────────────┐  │
│  │ 不限時 (3.0)  安靜 (2.8)          │  │
│  │ 有插座 (2.5)  wifi (2.5)          │  │
│  │ 早餐 (2.3)    午餐 (2.3)          │  │
│  │ 晚餐 (2.2)    甜點 (2.2)          │  │
│  │ 咖啡 (2.0)    ...                 │  │
│  └───────────────────────────────────┘  │
│           ↓ 最高優先級                   │
│                                         │
│  Tier 2 (9 個) - 權重: 1.0 ~ 1.9         │
│  ┌───────────────────────────────────┐  │
│  │ 文青 (1.8)    閱讀 (1.7)          │  │
│  │ 書店 (1.6)    音樂 (1.5)          │  │
│  │ 藝術 (1.4)    設計 (1.3)          │  │
│  │ ...                               │  │
│  └───────────────────────────────────┘  │
│           ↓ 中等優先級                   │
│                                         │
│  Tier 3 (6 個) - 權重: 0.5 ~ 1.0         │
│  ┌───────────────────────────────────┐  │
│  │ 環境 (1.0)    氛圍 (0.9)          │  │
│  │ 空間 (0.8)    裝潢 (0.7)          │  │
│  │ ...                               │  │
│  └───────────────────────────────────┘  │
│           ↓ 較低優先級                   │
│                                         │
└─────────────────────────────────────────┘

分數計算公式:
Score = Σ (關鍵字出現次數 × 關鍵字權重)

範例:
"不限時" 出現 2 次: 2 × 3.0 = 6.0
"安靜" 出現 1 次:   1 × 2.8 = 2.8
"文青" 出現 1 次:   1 × 1.8 = 1.8
總分 = 10.6
```

---

## 🗃️ 資料結構設計

### 1. Cafe (咖啡廳)
```java
class Cafe {
    String id;              // "cafe_001"
    String name;            // "羊毛與花"
    String url;             // 官網網址
    String district;        // "大安區"
    String address;         // 完整地址
    List<String> keywords;  // ["不限時", "安靜", "有插座"]
    List<String> features;  // ["不限時", "有插座", "有wifi"]
    List<String> hashtags;  // ["#不限時", "#安靜", "#文青"]
    String preview;         // 預覽文字
    double score;           // 搜尋分數（動態計算）
}
```

### 2. Keyword (關鍵字)
```java
class Keyword {
    String keyword;         // "不限時"
    double weight;          // 3.0
    KeywordTier tier;       // TIER1, TIER2, TIER3
    Map<String, String> translations;  // {"en":"unlimited time"}
}
```

### 3. SearchResult (搜尋結果)
```java
class SearchResult {
    Cafe cafe;                      // 咖啡廳物件
    double score;                   // 計算後的分數
    List<String> matchedKeywords;   // 符合的關鍵字
    List<String> hashtags;          // 生成的 hashtags
}
```

### 4. WebPage (網頁 - 用於多層爬取)
```java
class WebPage {
    String url;                 // 網頁 URL
    String name;                // 網頁標題
    double score;               // 網頁分數
    int depth;                  // 爬取深度 (0=母頁, 1=子頁, 2=孫頁)
    List<WebPage> children;     // 子網頁列表
}
```

---

## 💾 LocalStorage 資料結構

### 1. 收藏清單 (favorites)
```javascript
localStorage.setItem('favorites', JSON.stringify([
    {
        id: "cafe_001",
        name: "羊毛與花",
        district: "大安區",
        addedAt: "2025-12-15T13:00:00"
    },
    ...
]));
```

### 2. 瀏覽紀錄 (history)
```javascript
localStorage.setItem('history', JSON.stringify([
    {
        id: "cafe_001",
        name: "羊毛與花",
        visitedAt: "2025-12-15T13:05:00"
    },
    ...
]));
```

---

## 🔌 API 端點完整列表

```
GET  /api/search
     ?q=關鍵字
     回傳: 搜尋結果列表

GET  /api/search/advanced
     ?keyword=關鍵字&districts=地區&features=功能
     回傳: 進階篩選結果

GET  /api/recommendations
     ?districts=地區&features=功能
     回傳: 熱門推薦列表

GET  /api/filters/districts
     回傳: 所有可用地區列表

GET  /api/filters/features
     回傳: 所有可用功能列表

POST /api/error-reports
     Body: {cafeId, type, description}
     回傳: 回報成功訊息

GET  /api/cafes
     回傳: 所有咖啡廳列表（開發用）

GET  /api/keywords
     回傳: 所有關鍵字與權重（開發用）
```

---

## 🎓 資料結構與演算法應用

### 1. 快速排序 (QuickSort)
- **位置**: `Ranker.java`
- **用途**: 對搜尋結果按分數排序
- **時間複雜度**: O(n log n) 平均情況

### 2. 雜湊表 (HashMap)
- **位置**: `KeywordService.java`, `CafeRepository.java`
- **用途**: 快速查詢關鍵字權重、咖啡廳資料
- **時間複雜度**: O(1) 查詢

### 3. 樹狀結構 (Tree)
- **位置**: `WebTree.java`, `WebPage.java`
- **用途**: 表示網站多層結構（母頁→子頁→孫頁）
- **應用**: 多層網頁爬取

### 4. 陣列列表 (ArrayList)
- **位置**: 幾乎所有類別
- **用途**: 儲存咖啡廳列表、關鍵字列表、搜尋結果
- **優點**: 動態大小、快速隨機存取

### 5. 字串比對演算法
- **位置**: `ChineseTokenizer.java`, `KeywordParser.java`
- **用途**: 中文分詞、關鍵字匹配
- **應用**: 解析使用者輸入

---

## 🚀 效能優化

### 1. 快取機制
- **位置**: `CacheConfig.java`
- **用途**: 快取搜尋結果、關鍵字資料
- **效果**: 減少重複計算

### 2. 預計算
- **位置**: `baseline-scores.json`
- **用途**: 預先計算每家咖啡廳的基準分數
- **效果**: 加速推薦功能

### 3. 索引優化
- **位置**: `KeywordRepository.java`
- **用途**: 建立關鍵字索引
- **效果**: O(1) 關鍵字查詢

---

**系統已完整實作並串接完成，可以開始測試了！** 🎉
